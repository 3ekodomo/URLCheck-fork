Let's check how `AndroidUtils.setCustomBackground(View view, Context context)` works:
```java
static void setCustomBackground(View view, Context context) {
    GradientDrawable drawable = new GradientDrawable();
    drawable.setShape(GradientDrawable.RECTANGLE);

    // Element Color
    try {
        String colorStr = AndroidSettings.ELEMENT_COLOR_PREF(context).get();
        int color = Color.parseColor(colorStr);
        drawable.setColor(color);
    } catch (IllegalArgumentException e) {
        drawable.setColor(Color.WHITE); // Default if parse fails
    }

    // Edges Configuration
    boolean isRounded = AndroidSettings.EDGES_PREF(context).get() == AndroidSettings.Edges.ROUNDED;
    if (isRounded) {
        float amount = (float) AndroidSettings.ROUNDED_AMOUNT_PREF(context).get();
        float radius = TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, amount, context.getResources().getDisplayMetrics());
        drawable.setCornerRadius(radius);
    } else {
        drawable.setCornerRadius(0f);
    }

    // Border Configuration
    if (AndroidSettings.SHOW_BORDER_PREF(context).get()) {
        // stroke
    }
    view.setBackground(drawable);
}
```
If we want to split into Element and Interface borders:
For Elements (`AndroidUtils.setCustomElementBackground` or modify `setCustomBackground`):
1. Background color: ELEMENT_COLOR_PREF (default #ffffff).
2. Corners:
   - Edges.ROUNDED -> custom radius from ROUNDED_AMOUNT_PREF.
   - Edges.SHARP -> 0dp.
   - Edges.DEFAULT -> 3dp (like original `round_box.xml`).
3. Stroke: ONLY if SHOW_ELEMENT_BORDER_PREF is true.

For Interface Border (`MainDialog.java`, apply to `findViewById(R.id.middle_modules)`):
1. Background color: Transparent or don't set it (wait, it's just a border! The interface itself has a background, probably. But a stroke on a GradientDrawable with no solid color will be a hollow box with a border.)
   Let's create `AndroidUtils.setInterfaceBorder(View view, Context context)`.
   If SHOW_INTERFACE_BORDER_PREF is false, set background to null.
   If true:
   - Stroke: BORDER_COLOR_PREF, width 1.5dp.
   - Corners:
     - Edges.ROUNDED -> custom radius from ROUNDED_AMOUNT_PREF.
     - Edges.SHARP -> 0dp.
     - Edges.DEFAULT -> 0dp? Wait, original URLCheck UI has no outer border, so default interface container shape is just whatever it is (probably sharp or 0dp radius, let's use 0dp or not set it). Wait! The default interface container shape might be determined by the dialog window. If we set a background, we might override it. So if we just set stroke and transparent solid, we might need corner radius:
     - Edges.DEFAULT: preserve original container shape. Since original container had no border, setting a stroke on it with 0dp radius might look correct if the dialog itself is sharp, or if it's rounded we should match it. Actually, `middle_modules` is a LinearLayout, its default background is transparent (null).

Let's see what `MainDialog.java` does.
`ll_main = findViewById(R.id.main);`
`middle_modules = findViewById(R.id.middle_modules);`
If we apply `AndroidUtils.setInterfaceBorder(middle_modules, this);`

Wait, `ModulesActivity.java` also calls `AndroidUtils.setCustomBackground(parent, this);` for its modules!
And `DrawerModule.java` calls `setCustomBackground`.
So `setCustomBackground` is for elements.
I will change `SHOW_BORDER_PREF` to `SHOW_INTERFACE_BORDER_PREF`.
Wait, I shouldn't rename the PREF string "showBorder" because it might break existing settings, but it's fine for beta. Let's create `SHOW_INTERFACE_BORDER_PREF` saving to "showInterfaceBorder" and `SHOW_ELEMENT_BORDER_PREF` saving to "showElementBorders". And I can remove `SHOW_BORDER_PREF`.
Or keep `showBorder` for interface border.

Let's do:
```java
static BoolPref SHOW_INTERFACE_BORDER_PREF(Context cntx) {
    return new BoolPref("showInterfaceBorder", false, cntx);
}
static BoolPref SHOW_ELEMENT_BORDER_PREF(Context cntx) {
    return new BoolPref("showElementBorder", false, cntx);
}
```
And Edges:
```java
enum Edges implements Enums.IdEnum, Enums.StringEnum {
    DEFAULT(0, R.string.spin_edges_default),
    SHARP(1, R.string.spin_edges_sharp),
    ROUNDED(2, R.string.spin_edges_rounded),
    ;
```
Wait, if I change the enums ids, I should be careful not to break existing settings if it stores ID or name. `EnumerationPref` stores the enum name. So changing `ROUNDED` from id 0 to id 2 won't break if it saves as string. Wait, `EnumerationPref` uses `valueOf` for String.

Wait, original default color of `elementColor` was #ffffff and `borderColor` was #808080.
